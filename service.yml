apiVersion: v1
kind: Service
metadata:
  name: demo-app-service
spec:
  type: LoadBalancer  
  selector:
     app: demoapp-deployment
  ports:
   - protocol: "TCP"
     port: 8082
     targetPort: 8082
